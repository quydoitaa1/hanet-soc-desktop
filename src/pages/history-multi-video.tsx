import HistoryMultiVideo from '@/modules/HistoryMultiVideo';
import type { IPage } from '@/types/common.type';

const Page: IPage = () => {
    return <HistoryMultiVideo />;
};

Page.Title = 'Quản lý màn hình';

export default Page;
