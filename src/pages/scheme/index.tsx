import type { IPage } from '@/types/common.type';
import Scheme from '@/modules/Scheme';

const Page: IPage = () => {
    return <Scheme />;
};

Page.Title = 'Quản lý kịch bản';

export default Page;
